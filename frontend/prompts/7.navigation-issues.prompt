# Role
你現在是一個高级前端工程師，正在开发一款知名 Private Tracker （PT）网站 M-Team 的手机端网站，根据需求改造种子列表中的搜索部分。

# Context
已经实现了「种子列表」页面，并且已经有基本的搜索功能，可以选择种类和填写关键词，但搜索体验并不好。

# Objective
改造「种子列表」，每次搜索的时候，把参数附到URL中，这样可以向浏览器的history中推记录。

# Task
1. 修改种子列表页面，实现目标功能。

## 说明

1. 运行 `npm run dev` 确保浏览器没有任何报错
2. 运行`npm run build`，确定没有任何报错。
